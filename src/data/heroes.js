export const HEROES = {
    'warrior': {
        name: 'Warrior',
        texture: 'hero_sprite', // Must match ASSETS.HERO key in BootScene
        scale: 0.7,    // Scale override (1.0 = default fit)
        yOffset: -10     // Vertical offset (positive = down, negative = up)
    }
};
